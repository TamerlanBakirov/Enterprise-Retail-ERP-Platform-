<UserControl x:Class="GeorgiaERP.Desktop.Views.Products.ProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Loaded="OnLoaded">

    <DockPanel Margin="24,16">
        <!-- Header -->
        <DockPanel DockPanel.Dock="Top" Margin="0,0,0,16">
            <TextBlock Text="Products" FontSize="22" FontWeight="SemiBold"
                       Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center" />

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBox Style="{StaticResource ModernTextBox}" Width="250"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
                <Button Content="Search" Style="{StaticResource PrimaryButton}"
                        Command="{Binding SearchCommand}" Margin="8,0,0,0" />
            </StackPanel>
        </DockPanel>

        <!-- Pagination -->
        <Border DockPanel.Dock="Bottom" Padding="0,12">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Previous" Style="{StaticResource SecondaryButton}"
                        Command="{Binding PreviousPageCommand}" Margin="0,0,8,0" />
                <TextBlock VerticalAlignment="Center" Margin="8,0">
                    <Run Text="Page " /><Run Text="{Binding CurrentPage}" />
                    <Run Text=" of " /><Run Text="{Binding TotalPages}" />
                    <Run Text=" (" /><Run Text="{Binding TotalCount}" /><Run Text=" items)" />
                </TextBlock>
                <Button Content="Next" Style="{StaticResource SecondaryButton}"
                        Command="{Binding NextPageCommand}" Margin="8,0,0,0" />
            </StackPanel>
        </Border>

        <!-- Loading -->
        <ProgressBar DockPanel.Dock="Top" IsIndeterminate="True" Height="3"
                     Foreground="{StaticResource PrimaryBrush}"
                     Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}" />

        <!-- Data grid -->
        <Border Style="{StaticResource Card}" Padding="0">
            <DataGrid Style="{StaticResource ModernDataGrid}"
                      ItemsSource="{Binding Products}"
                      SelectedItem="{Binding SelectedProduct}"
                      ColumnHeaderStyle="{StaticResource ModernDataGridColumnHeader}"
                      RowStyle="{StaticResource ModernDataGridRow}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="SKU" Binding="{Binding Sku}" Width="100" />
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" />
                    <DataGridTextColumn Header="Category" Binding="{Binding CategoryName}" Width="130" />
                    <DataGridTextColumn Header="Barcode" Binding="{Binding Barcode}" Width="130" />
                    <DataGridTextColumn Header="Price" Binding="{Binding RetailPrice, StringFormat=N2}" Width="100" />
                    <DataGridTextColumn Header="VAT" Binding="{Binding VatRate, StringFormat=P0}" Width="60" />
                    <DataGridTextColumn Header="Unit" Binding="{Binding UnitOfMeasure}" Width="60" />
                    <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" Width="60" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </DockPanel>
</UserControl>
